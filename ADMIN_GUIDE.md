# 🏸 LINE 羽球接龍機器人 — 管理員手冊

> 本文件適用對象：**管理員（已取得接龍密碼）**
> 一般群友使用說明請見 → [README.md](./README.md)

---

## 目錄

1. [取得管理員權限](#1-取得管理員權限)
2. [建立場次](#2-建立場次-接龍開始)
3. [修改場次](#3-修改場次-接龍修改)
4. [結束 / 清空場次](#4-結束--清空場次)
5. [查詢與推播名單](#5-查詢與推播名單)
6. [群組代碼功能](#6-群組代碼功能)
7. [跨群組操作](#7-跨群組操作)
8. [繳費狀態管理（LIFF 大廳）](#8-繳費狀態管理liff-大廳)
9. [超級清空（緊急用）](#9-超級清空緊急用)
10. [注意事項](#10-注意事項)

---

## 1. 取得管理員權限

在任意群組（或私訊機器人）輸入以下通關密語：

```
接龍密碼 Tony好帥
```

> 空格可有可無，`接龍密碼Tony好帥` 也有效。

機器人回覆：`✅ 權限已開通！您現在是本群組的管理員了。`

**管理員是「全域」的**，不限群組。只要輸入過一次密碼，任何群組的場次您都有完整操作權限。

---

### 取消管理員身分

```
取消管理員
```

---

## 2. 建立場次（接龍開始）

### 基本格式

```
接龍開始
日期：星期二
時間：晚6~8
地點：南港運動中心
費用：230
人數{21}
候補{5}
名單{Tony(高階), Kay-新手, 小明}
標籤{中階場}
備註：請自備零錢
發布時間{2026/07/01 18:00}
提醒時間{2026/07/01 17:30}
```

### 所有欄位說明

| 欄位 | 說明 | 範例 | 必填 |
|------|------|------|------|
| `日期` | 活動日期 | `日期：星期二` | 否 |
| `時間` | 活動時間段 | `時間：晚6~8` | 否 |
| `地點` | 活動地點 | `地點：南港運動中心` | 否 |
| `費用` | 每人費用 | `費用：230` | 否 |
| `標題` | 自訂標題（不填則自動組合日期時間地點） | `標題{週二夜場}` | 否 |
| `人數` | 正取名額（預設 20） | `人數{21}` | 否 |
| `候補` | 候補名額（預設 5） | `候補{5}` | 否 |
| `名單` | 預設已報名名單（逗號或換行分隔） | `名單{Tony, Kay}` | 否 |
| `匿名名單` | 匿名佔位（數字或名稱） | `匿名名單{3}` | 否 |
| `標籤` | 顯示在大廳卡片頂部的灰色標籤 | `標籤{微初至中階}` | 否 |
| `備註` | 附加說明文字 | `備註：請自備零錢` | 否 |
| `發布時間` | 定時自動推播大廳連結 | `發布時間{2026/07/01 18:00}` | 否 |
| `提醒時間` | 定時推播「活動即將開始」提醒 | `提醒時間{2026/07/01 17:30}` | 否 |

> 💡 所有欄位都支援大括號（`{}`）或冒號（`:`）兩種格式，例如 `日期{星期二}` 與 `日期：星期二` 效果相同。

### 預設名單中指定程度

名單內的名字支援以下格式帶入程度：

| 格式 | 說明 |
|------|------|
| `Tony(高階)` | 括號格式 |
| `Tony（高階）` | 全形括號 |
| `Tony-新手` | 減號格式 |
| `Tony[進階]` | 方括號格式 |

### 預設名單中標記已繳費

在名字後面加上 `$` 或 `(已繳費)` 即可標記為已繳費狀態：

```
名單{Tony$, Kay(已繳費), 小明}
```

### 建立跨群組場次

在另一個群組建立場次並直接推播過去：

```
接龍開始
群組{5821}
日期：星期四
地點：玉成國小
```

> 需要先在目標群組輸入 `群組代碼` 取得代碼，才能使用此功能。

---

## 3. 修改場次（接龍修改）

```
接龍修改 關鍵字 欄位：新內容
```

| 範例 | 說明 |
|------|------|
| `接龍修改 星期二 地點：新興國小` | 修改含「星期二」的場次地點 |
| `接龍修改 南港 費用：250` | 修改含「南港」的場次費用 |
| `接龍修改 人數{25}` | 若只有一個場次，可省略關鍵字 |
| `接龍修改 標題{週三晚間場}` | 修改標題 |
| `接龍修改 備註：更改為 7 點半開打` | 修改備註 |

可修改的欄位同建立場次，包含日期、時間、地點、費用、人數、候補、標題、標籤、備註、名單、發布時間、提醒時間。

---

## 4. 結束 / 清空場次

| 指令 | 效果 |
|------|------|
| `接龍結束 星期二` | 關閉並移除含「星期二」的場次 |
| `接龍清空` | 清空目前群組所有場次 |
| `接龍清空 群組{5821}` | 清空指定代碼群組的所有場次 |

---

## 5. 查詢與推播名單

| 指令 | 效果 |
|------|------|
| `接龍名單` | 回傳目前群組所有場次的名單 |
| `接龍名單 星期二` | 只回傳含「星期二」的場次名單 |
| `接龍名單 群組{5821}` | 查詢代碼 5821 的群組名單並**推播到那個群組** |
| `接龍名單 群組{5821} 星期四` | 查詢並篩選關鍵字後推播到遠端群組 |

---

## 6. 群組代碼功能

每個群組都有一個 **4 位數專屬代碼**，用來支援跨群組操作。

| 指令 | 效果 |
|------|------|
| `群組代碼` 或 `群組碼` | 查詢（或自動產生）本群組的代碼 |

> ✅ 代碼一旦產生即**永久固定**，伺服器重啟也不會消失（已備份至 GitHub）。

---

## 7. 跨群組操作

您可以從私訊或 A 群，操作 B 群的場次。

| 情境 | 指令範例 |
|------|----------|
| 在 A 群幫 B 群建立場次 | `接龍開始`（加上 `群組{B群代碼}`） |
| 從私訊清空 B 群場次 | `接龍清空 群組{B群代碼}` |
| 從私訊推播 B 群名單 | `接龍名單 群組{B群代碼}` |
| 一次推播多個群（重複發送）| 分別發 `接龍名單 群組{A代碼}` 和 `接龍名單 群組{B代碼}` |

---

## 8. 繳費狀態管理（LIFF 大廳）

1. 點擊大廳連結進入 LIFF 頁面
2. 選擇場次 → 進入名單明細
3. 每個人名字旁邊會出現 **「⬜ 未繳費」** 或 **「💰 已繳費」** 按鈕
4. 點擊即可切換狀態

> 只有管理員看得到此按鈕並可操作，一般群友只能看到已繳費的標記。

**初始繳費狀態**：可在 `接龍開始` 的 `名單{}` 欄位用 `$` 或 `(已繳費)` 標記。

---

## 9. 超級清空（緊急用）

```
超級清空
```

**⚠️ 危險操作**：會刪除伺服器上**所有群組**的**所有場次**，無法復原，請謹慎使用。

---

## 10. 注意事項

- 一般群友輸入任何指令，機器人**一律已讀不回**（保護防洗版）
- `群組代碼`、`群組碼` 這兩個指令任何人都可以使用（用來取得代碼）
- `接龍密碼 Tony好帥` 任何人輸入都有效，請勿外洩密碼
- 管理員身分為**全域**，一次輸入密碼，所有群組都有管理員功能
- 若要撤銷管理員身分，自行輸入 `取消管理員`

---

*最後更新：2026-06-25*
